# Codeception Test Suite Configuration
#
# Suite for unit or integration tests that require WordPress functions and classes.
actor: WpunitTester
modules:
  enabled:
    - Asserts
    - WPLoader
    - \Tests\WPGraphQL\FacetWP\Helper\Wpunit
  disabled:
    - WPDb
    - WPBrowser
  config:
    WPDb:
      cleanup: false
